import ContentCard from "./ContentCard";

const ContentSection = () => {
  return (
    <section className="p-8" id="open">
      <ContentCard />
    </section>
  );
};

export default ContentSection;
